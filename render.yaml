services:
  - type: web
    name: dreamboard
    env: python
    buildCommand: ""
    startCommand: "gunicorn dreamboard.wsgi:application"
    build:
      commands:
        - pip install -r requirements.txt
        - python manage.py collectstatic --no-input
        - python manage.py migrate
